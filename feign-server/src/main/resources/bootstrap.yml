spring:
  application:
    name: fiegn-server
  cloud:
    config:
      retry:
        initial-interval: 2000
        max-attempts: 3
      discovery:
        enabled: true
        service-id: config-server
  profiles:
    active: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://user:123456@localhost:8761/eureka/